# .env.example

# A 32-character cryptographically secure random string — replace before deployment
SECRET_KEY=replace_with_a_real_secret_key_32chars_minimum

# Bearer token or API key for external service
API_TOKEN=replace_with_actual_api_token
